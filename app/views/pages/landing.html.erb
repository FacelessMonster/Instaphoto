<% unless user_signed_in?%>
<div class=" container jumbotron">
  <h1 class="display-4 google-font-2"><b><%= t('landing.header') %></b></h1>
  <p class="lead"><%= t('landing.description') %></p>
  <hr class="my-4">
  <p><%= t('landing.cursor') %></p>
  <%= link_to t('landing.login'), new_user_session_path, class: "btn btn-success" %>
  <%= link_to t('landing.sign_up'), new_user_registration_path, class: "btn btn-success" %>
</div>
<%end%>
