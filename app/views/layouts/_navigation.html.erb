<div class="container">

  <nav class="navbar navbar-expand-lg navbar-light ">
    <%=link_to 'Instaphoto', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%=link_to t('navigation.about'), pages_about_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%=link_to t('navigation.feed'), profiles_friends_photos_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%=link_to t('navigation.my_photos'), profiles_my_photos_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to t('navigation.new_photo'), new_photo_path, class: "nav-link" %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= current_user.username %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <%=link_to t('navigation.subscribes'), profiles_subscribes_list_path, class: "nav-link active destroy_session" %>
              <%=link_to t('navigation.profile'), edit_user_registration_path, class: "nav-link active destroy_session" %>
              <%=link_to t('navigation.exit'), destroy_user_session_path, class: "nav-link active destroy_session", method: :delete %>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>

</div>
